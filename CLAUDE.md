# 작업 규칙 (Claude Code 세션 공통)

이 저장소는 핸디즈 과제 제출용이다. 평가 축은 구현 완성도 · 문제 정의 · 범위 판단 · **AI 활용 및 검토 과정** 4개이며,
마지막 축의 근거 자료로 `AI_LOG.md` 를 작업 중에 실시간으로 쌓는다.

## 기록은 두 층이다

- `AI_PROMPTS.md` — 사용자가 입력한 **프롬프트 전문**. `.claude/settings.json` 의 `UserPromptSubmit` 훅
  (`.claude/hooks/log-prompt.sh`) 이 자동으로 append 한다. AI 가 손대지 않는다 (수정·정리 금지). **gitignore 대상, 커밋하지 않는다.**
- `AI_LOG.md` — 작업 단위별 **요약·검토·결과**. AI 가 아래 규칙대로 수동으로 쓴다.

## AI_LOG.md 기록 규칙 (필수)

**의미 있는 작업 단위가 끝날 때마다** `AI_LOG.md` 맨 아래에 항목을 하나 추가한다.
"의미 있는 단위" = 문제·범위 결정, 설계 선택, 파일 생성/수정, 테스트 실행, 버그 수정, README 작성 등.
사용자가 따로 요청하지 않아도 기록한다. 세션이 끝나기 전에 미기록 항목이 없는지 확인한다.

항목 형식:

```
### YYYY-MM-DD HH:MM — <단계명>
- **요청**: 사용자가 AI 에게 시킨 것 (한 줄, 실제 표현에 가깝게)
- **AI 출력**: AI 가 만든/제안한 것 (파일·함수·결정 단위로)
- **검토**: 사용자가 어떻게 확인했는가 (실행 · 테스트 · 코드 읽기 · 수동 재현 등 구체 행동)
- **결과**: 채택 / 수정 후 채택 / 폐기 — 그리고 **이유**
```

기록 원칙:

- 꾸미지 않는다. AI 가 틀린 것, 사용자가 되돌린 것, 버린 제안을 **그대로** 적는다. 이 로그의 가치는 실패 기록에 있다.
- "검토" 는 추상어("확인함") 금지. 무엇을 돌렸고 무엇을 봤는지 동작으로 쓴다.
- 사용자의 판단과 AI 의 판단을 구분한다. 누가 결정했는지 애매하면 사용자에게 묻고 기록한다.
- 시각은 로컬 시각(KST) 기준, `date "+%Y-%m-%d %H:%M"` 로 찍는다.

## 범위 원칙

- 과제문이 "**작은** 결과물" 이라고 명시했다. 범위 확장 제안은 하지 않는다. 필요하면 "뺀 것" 목록으로 README 에 넘긴다.
- `_notes.local/` 은 gitignore 된 로컬 메모다. 내용을 코드·README·커밋에 옮기지 않는다.
- 제출용 `README.md` 는 절 구성을 유지한다: 문제 정의 / 범위 판단 / 설계·구현 / AI 활용 및 검토 / 실행 방법.

## 응답 스타일

- 두괄식. 결론 먼저, 근거는 아래.
- 원문자(①②③) 사용 금지. 일반 숫자 목록을 쓴다.
