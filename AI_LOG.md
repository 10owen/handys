# AI 활용 로그

> 이 과제에서 AI(Claude Code)를 어디에 썼고, 그 출력을 어떻게 검토했으며, 무엇을 버렸는지를
> **작업 순서 그대로** 기록한 원장이다. 제출용 `README.md` 의 "AI 활용 및 검토" 절은 이 로그를 요약한 것이다.
> 사후에 정리해 쓴 것이 아니라 작업 중 실시간으로 쌓았다. 실패·폐기 항목도 그대로 남긴다.

**형식**: 단계마다 `요청 → AI 출력 → 검토 → 결과(채택/수정/폐기 + 이유)`.
기록 규칙은 저장소의 `CLAUDE.md` 에 있다 (AI 세션에 주는 지시문 자체도 AI 활용 방식의 일부라 함께 커밋한다).

---

### 2026-09-17 — 착수: AI 활용 기록 방식 결정
- **요청**: "클로드 코드로 과제를 진행할 건데, 진행 방식을 기록해 달라. AI 활용을 어떻게 서술해야 할지 고민된다."
- **AI 출력**: (1) 작업 중 실시간으로 쌓는 원장 `AI_LOG.md` 와 (2) 모든 세션이 같은 형식으로 기록하도록 강제하는
  저장소 지시문 `CLAUDE.md` 를 제안·생성. 항목 형식을 `요청/AI 출력/검토/결과` 4칸으로 고정.
- **검토**: 사용자가 두 파일 내용을 읽고 형식 승인 여부 판단. 2026-09-17 14:38 사용자가 "네 그렇게 진행해주세요" 로 승인.
- **결과**: 수정 후 채택. 4칸 형식 자체는 승인됐으나, 전문 기록 층이 빠져 있어 아래 항목에서 보완함.
  - (2026-09-17 14:38 추기) 사용자가 "프롬프트 등을 기록하게 되어 있나" 질문 → AI 가 세팅을 설명 → 사용자가
    "요약도 남기고 전문도 남겨 달라" 고 요구. 요약 층(`AI_LOG.md`)은 유지하되 전문 층이 빠져 있었던 것이
    이 설계의 누락. 아래 항목에서 보완.

### 2026-09-17 14:38 — 프롬프트 전문 자동 기록 훅 추가
- **요청**: "제가 활용한 프롬프트 등에 대해 기록하게끔 되어있나요? 세팅내용을 알려주세요" →
  "요약도 남겨주시고 전문도 남겨주세요"
- **AI 출력**:
  - 첫 질문에 대한 답: 프롬프트 원문 자동 저장 장치는 없고 `AI_LOG.md` 요약만 있다고 설명.
    전문이 필요하면 (a) 세션 transcript JSONL 에서 사후 추출 또는 (b) `UserPromptSubmit` 훅으로 자동 append 를 제안.
  - 사용자가 "전문도" 요구 → (b) 채택. 생성물:
    - `.claude/hooks/log-prompt.sh`: stdin JSON 의 `prompt` 를 시각·세션ID 와 함께 `AI_PROMPTS.md` 에 append
    - `.claude/settings.json`: `UserPromptSubmit` 훅 등록 (프로젝트 설정, 커밋 대상)
    - `AI_PROMPTS.md`: 원문 로그 파일. 훅 설치 전 프롬프트 2건은 수동으로 옮겨 적음
    - `CLAUDE.md`: "기록은 두 층이다" 절 추가 (전문 = 훅 자동 / 요약 = AI 수동)
- **검토**: AI 가 가짜 JSON 을 파이프해 훅 스크립트를 2회 실행 — 1회차는 AI 가 만든 테스트 입력 자체가
  잘못된 JSON(`\n` 미이스케이프)이라 jq 파싱 에러, 입력을 고쳐 2회차 통과. `jq -e` 로 settings.json 구조 검증.
  테스트로 생긴 항목은 `AI_PROMPTS.md` 에서 제거. **실제 세션에서 훅이 발화하는지는 사용자의 다음 프롬프트
  입력 후에야 확인 가능** (이 세션 시작 시점에 `.claude/` 가 없었으므로 `/hooks` 를 한 번 열거나 재시작이 필요할 수 있음).
- **결과**: 채택 (사용자 지시).
  - (2026-09-17 14:38 추기) 사용자의 다음 프롬프트("네 그렇게 진행해주세요")가 `AI_PROMPTS.md` 에 자동으로 붙은 것을
    AI 가 `tail` 로 확인. `/hooks` 재로드 없이 발화됨 — 우려했던 설정 감시 문제는 없었다.

### 2026-09-17 14:40 — 문제 정의·해법 아이디어 제시 (사용자) 와 범위 평가 (AI)
- **요청**: 사용자가 아이디어를 서술. 문제 = "숙박 예약에서 예약자(결제자)와 의사결정자가 다른 경우가 많고,
  의사결정자 취향에 맞는 숙소를 골라오는 일은 시간·노력이 큰 별도 작업이다. 이를 개선하면 결제자 경험과
  PU retention 을 잡을 수 있다." 해법 = (1) 인스타그램 연동: 숙소 사진 AI variation 생성 + 인스타 직접 업로드 +
  로그인 정보로 예약자·의사결정자 preference 수집. (2) 친구 기능: 에어비앤비 공동 favorite 벤치마크, (1)에서 모은
  preference 로 후보를 취향순 정렬 + "어떤 부분이 당신과 맞는지" 안내 + 순위 수동 수정 + 여행 단위 후보군 +
  멤버별 여행 히스토리.
- **AI 출력**: 문제 정의는 그대로 채택 권고. 해법 (1)은 통째로 "일부러 뺀 것"으로 README 에 넘기고, 해법 (2)를
  "여행 단위 공동 후보 목록 + 멤버 취향 입력 + 취향 적합순 정렬 + 근거 표시" 한 덩어리로 좁힐 것을 권고.
  근거: 마감(09/18 23:59)까지 약 1.5일이고 구현 미착수, 과제문의 "작은 결과물"과 "범위 판단" 평가축,
  인스타 연동은 API 제약(개인 계정 데이터 접근 불가)·개인정보 목적외 수집·숙소 사진 AI 변형의 표시 신뢰 문제로
  하루 안에 닫을 수 없음. 히스토리·수동 순위 수정·인증·실데이터 연동은 2차로 미룸.
- **검토**: 사용자 판단 대기. AI 는 로컬 브리프 메모(gitignore 대상)의 마감·타임박스를 읽고 근거로 삼음.
- **결과**: 미정 — 사용자가 범위를 확정하면 이 항목 아래에 추기.

### 2026-09-17 14:46 — 해법 1 대체안: 핸디즈 자체 "여행 취향 테스트" 링크 (사용자)
- **요청**: 인스타 대신 핸디즈 자체 기능으로. 심리테스트처럼 링크를 공유하면 상대가 여행 취향을 입력 →
  결과 표시 → 회원가입/로그인 유도. 취향은 처음엔 추상 수준(뷰 중시)으로 받되 세분화 가능(바다/산/산속/숲 조망).
  이용 누적 시 여행 기록으로 취향을 더 얻고, "작년 산 여행 vs 올해 산 여행 중 뭐가 좋았나 + 이유 한 줄" 식
  비교 질문으로 라벨링.
- **AI 출력**: 인스타 안의 세 문제(API 불가·목적외 수집·사진 변형)가 모두 해소되므로 채택 권고. 다만 "테스트 →
  결과 → 가입 → 기록 기반 정교화 → 비교 라벨링" 은 로드맵이므로 결과물은 앞 두 칸(취향 테스트 + 후보 정렬)으로
  한정 권고. 형태는 CLI 보다 단일 정적 HTML 이 적합(링크 공유·질문 흐름이 UI 이므로). 가입·기록·비교 라벨링은
  "더 하면 할 것".
- **검토**: 사용자 판단 대기.
- **결과**: 미정.

### 2026-09-17 15:35 — "문제와 해법이 실제로 동작하는가" 관점 재검토 (사용자 요청)
- **요청**: "의사결정자 취향 숙소 추천 → 결제자 경험 개선 → 재결제" 인과가 해법으로 실제 동작하는지 검토해 달라.
- **AI 출력**: 인과 사슬을 4단계 가정으로 분해하고 약한 고리 3개를 지적. (a) 자기보고 취향 ≠ 실제 만족(맥락 의존:
  연인/친구/가족 여행마다 취향이 다름) → "이번 여행" 단위로 묻고 세부값·사후 라벨링으로 보정. (b) 예약자가 손으로
  고른 후보를 재정렬하는 것은 "고르는 노동"을 없애지 못함 → 취향 프로필로 카탈로그 전체에서 후보를 뽑는 프레이밍으로
  전환 권고(구현은 같은 점수 함수). (c) 재결제 레버는 "한 번 잘 고름"이 아니라 "취향 프로필이 핸디즈에 쌓임"이므로
  README 에 그렇게 써야 함. 추가: 취향 축(바다뷰/산속)이 핸디즈 실제 재고와 맞는지 미확인, 숙소 태깅은 운영 의존,
  성공 지표(테스트 완료율·추천 후보 예약율·재예약율) 명시 권고, 의사결정자가 테스트를 안 할 때 대리 입력 fallback.
- **검토**: 사용자 판단 대기.
- **결과**: 미정.

### 2026-09-17 23:44 — "문제가 너무 서브셋 아닌가" 재점검 (사용자 질문)
- **요청**: 정의한 문제(예약자≠의사결정자)와 해법이 너무 서브셋 같다. 꼭 핸디즈 타깃 시장만 다룰 필요도 없지 않나.
- **AI 출력**: (1) 이 과제에서 "서브셋"은 약점이 아니라 범위 판단 축의 가점 요인. 문제는 고객의 서브셋이되 지표(앱
  직접예약·재방문)는 핵심이므로 "좋은 종류의 서브셋". (2) 과제문은 도메인만 지정하므로 핸디즈 밖 문제도 허용되나,
  평가자가 핸디즈 사람이고 마감이 하루 남아 전환 비용이 크다 → 유지 권고. (3) 서브셋 느낌을 줄이는 방법은 범위
  확대가 아니라 프레이밍: 문제는 "공동 의사결정 숙소 예약"으로 일반화해 쓰고, 범위 절에서 커플·친구·가족 레저로
  좁힌다. 결과물은 동일. (4) 다른 문제로 바꾸려면 두 문장 정의·지표 하나·하루 구현 세 조건을 통과해야 한다는 기준 제시.
- **검토**: 사용자 판단 대기.
- **결과**: 미정.

### 2026-09-18 00:02 — 문제 정의 문장 확정 초안 + 해법 후보 5개 비교
- **요청**: 문제를 간결하게 정리하고, 사용자 안을 포함해 다른 해법도 추천받고 싶다.
- **AI 출력**: 문제 정의 3문장 초안("결제자≠판단자인 공동 의사결정인데 예약 흐름은 결제자 한 명만 상대한다 →
  동행자 취향을 앱 안으로 → 취향이 쌓여 재예약"). 해법 5개 비교표: A 취향 테스트 링크(사용자 안), B 공동 후보 보드,
  C 동행자 관점 비교 카드, D 체크아웃 후 동행자 피드백, E 역할 분리. 권장 = A 본체 + B 의 "답을 URL 로 돌려보내기"
  장치(서버 없이 공유 흐름 시연) + D 는 README 다음 단계 + C·E 는 검토 후 뺀 것. 결과물 범위 4항목 확정 제안
  (동행 선택 → 질문 5~6개(대리 입력) → 공유 링크 왕복 → 시드 10~12개 점수화·정렬·근거, 정적 HTML 1개).
- **검토**: 사용자 판단 대기. 마감 당일(09/18)임을 AI 가 명시.
- **결과**: 미정.

### 2026-09-18 00:38 — AI 가 누락한 설계 복원: 여행 단위 공동 후보 보드 + 탈락 데이터 학습 (사용자 지적)
- **요청**: 에어비앤비 공동 favorite 벤치마크가 빠졌다. 여행 단위 멤버 초대 → 후보군 공동 열람 → 탈락 숙소의
  특징 + 유저 피드백으로 "이 여행이 추구하는 키워드" 추출 → 재활용.
- **AI 출력**: 누락을 인정. 탈락 행동은 "드러난 취향"이라 취향 테스트(자기보고)보다 강하고, 앞서 지적한 약한 고리
  (자기보고≠실제)를 정면 해결한다고 판단해 본체를 이 설계로 교체 권고. 취향 테스트는 콜드스타트 보조로 강등 또는
  제외(제외 권고). 오늘 크기 절단안: 한 브라우저 멤버 전환 시연, 시드 10~12개, 유지/탈락 + 이유 태그 + 자유 한 줄,
  태그 가중 합으로 키워드 산출, "다음 여행" 에서 재정렬. 초대 링크·동기화·실데이터·자유문장 분석·사후 피드백은 뺀 것.
- **검토**: 사용자가 AI 의 이전 권장안(취향 테스트 본체)을 읽고 자기 설계와 대조해 누락을 잡아냄.
- **결과**: AI 의 직전 권장안(테스트 본체 + 링크 왕복)은 폐기. 이유: 사용자 설계가 더 강한 신호(행동 데이터)를 쓰고
  에어비앤비 대비 차별점이 명확함. 탈락 이유를 태그 선택지로 받는 방식은 사용자 확인 대기.

### 2026-09-18 00:49 — 최종 설계 확정: 3층 취향 데이터 (사용자 종합)
- **요청**: 취향 테스트(기본 데이터) + 여행 방 세팅 시 키워드(지역·목적 → 사전 추천으로 공수 절감) + 후보 선별 행동
  (추가 데이터)을 합치면 "특정 유저 × 특정 여행" 취향으로 분화되어 더 높은 수준의 추천이 가능하다.
- **AI 출력**: 세 층(유저 기본 취향 / 여행 컨텍스트 / 여행 내 행동 학습)을 같은 태그 어휘로 통일하고, 추천 점수를
  세 층의 가중 합으로 정의. 구현 착수 선언: 정적 HTML 1개 + 시드 JS, localStorage, 멤버 전환 시연, 가상 숙소 12개.
- **검토**: 사용자가 종합 문장으로 설계를 확정. AI 는 마감 당일이라 추가 질문 없이 착수.
- **결과**: 채택. 이후 항목은 구현 단위로 기록.

### 2026-09-18 00:55 — 구현: 정적 HTML 앱 1차 (app/index.html · app/seed.js)
- **요청**: 확정 설계(3층 취향 데이터 · 여행 방 · 유지/탈락 학습 · 다음 여행 재활용)를 오늘 안에 돌아가는 형태로.
- **AI 출력**:
  - `app/seed.js`: 태그 어휘 5범주 19개, 가상 숙소 12개(핸디즈 재고 분포를 본뜸), 취향 테스트 5문항, 동행/목적
    키워드→태그 가중치, 유지/탈락 이유 15개(고정 가중치 또는 "그 숙소의 해당 범주 태그"에 가중치).
  - `app/index.html`: 단일 파일, 빌드 없음, localStorage. 멤버 추가(대리 입력 체크) → 취향 테스트 → 여행 만들기
    (누구와·인원·지역·목적·멤버) → 컨텍스트로 후보 5개 사전 추천 → 보드에서 유지/탈락 + 이유 + 한 줄 → "이 여행이
    추구하는 키워드"(전체·멤버별) → 정렬 갱신 → 카탈로그에서 더 담기 → "이 멤버로 다음 여행"에서 이전 여행 학습 반영.
  - 점수 = Σ멤버 기본취향 + Σ멤버 이전여행 학습 + Σ멤버 이번 선별 학습 + 여행 컨텍스트. 후보마다 "맞음/어긋남" 태그와
    출처(멤버명·이전여행·이번선별·여행목적) 표시. 탈락 한 표는 그 숙소의 모든 태그에 −1 + 이유 가중치(사용자가 말한
    "탈락 숙소의 특징 데이터 + 피드백" 그대로).
- **검토**: AI 가 Chrome 에서 실제로 돌림 — 멤버 2명 생성(오웬: 도심·야경·외식 / 지민(대리): 바다뷰·자차·해먹기),
  "10월 부산 커플 휴양·맛집" 생성 → 부산·2인 조건에 맞는 3개가 점수순(+20/+18/+18)으로 담김 → 지민이 서면을
  "너무 시내" 로 탈락 → 키워드 패널에 도심상권 −3·교외조용 +1 표시되고 순위 갱신 → 오웬이 해운대를 "뷰" 로 유지 →
  "다음 여행" 생성 시 근거에 "오웬·이전여행" 출처가 붙고 바다뷰 숙소가 상위. 콘솔 에러 0.
  발견한 문제 1건: 근거 줄의 출처 목록이 반대 방향 기여자까지 섞어 보여줌(예: 역세권 +4 옆에 −1 을 준 지민 표시) →
  순가중치와 같은 방향 출처만 표시하도록 수정.
- **결과**: 채택. 사용자 미검토 — 사용자가 직접 열어 흐름을 볼 것.

### 2026-09-18 00:56 — 제출용 README 초안
- **요청**: (사용자 요청 없이 CLAUDE.md 규칙에 따라) 5개 절 구성의 README 작성.
- **AI 출력**: `README.md` — 문제 정의(결제자≠판단자, 대상 고객, 지표) / 범위 판단(넣은 것 3층 표, 뺀 것 7개와 이유,
  다음 단계, 성공 지표) / 설계·구현(점수 함수·학습·재활용·트레이드오프) / AI 활용 및 검토(AI_LOG 요약 — AI 안 폐기 사례,
  AI 실수 2건 포함) / 실행 방법.
- **검토**: 사용자 미검토. 특히 4절은 "사용자 판단 vs AI 판단" 구분을 AI 가 로그에서 재구성한 것이라 사용자가 사실 관계를
  확인해야 한다.
- **결과**: 초안. 사용자 검토 대기.

### 2026-09-18 01:06 — docker-compose 로 띄우기
- **요청**: "docker-compose.yml 을 이용해서 간단히 띄울 수 있게" + 취향 키워드 추출·저장·활용 인프라를 어떻게 할지 의견.
- **AI 출력**: `docker-compose.yml` — nginx alpine 이 `./app` 을 읽기 전용으로 8080 에 서빙. README 실행 절 갱신.
  인프라는 설계안만 제시(이벤트 원본 append-only + 규칙 버전이 붙은 파생 프로필 테이블 + 프로필 조회 API). 구현 여부는
  마감 당일이라 사용자 결정에 맡김.
- **검토**: AI 가 `docker compose up -d` 실행 → `/` 와 `/seed.js` 모두 HTTP 200 확인 → `down`.
- **결과**: compose 채택. 인프라는 사용자 결정 대기.

### 2026-09-18 12:39 — 취향 인프라: 설계만 README 에, 구현은 안 함 (사용자 결정)
- **요청**: "작은 결과물을 가지고 오라 했으니 구현 결과물(지금 앱)을 가져가는 게 좋겠다. 보여주면서 설명하기에도 좋다."
- **AI 출력**: README 범위 판단 절에 "취향 데이터 인프라(설계만)" 단락 추가 — 원본 이벤트 append-only / 규칙 버전이 붙은
  파생 프로필 / 조회 API 2개. 지금 프론트의 localStorage·seed.js·재계산이 각각 어느 계층에 대응하는지 명시.
- **검토**: 사용자가 두 안(A 설계만 / B 최소 API 구현) 중 A 를 선택. AI 권장과 일치.
- **결과**: 채택.

### 2026-09-18 13:17 — 인프라 구현: MySQL + FastAPI + nginx (사용자 결정으로 A→B 전환)
- **요청**: "DB 구성으로 내 생각을 더 잘 전달할 수 있을 것 같다. Kotlin/Spring 은 내 스택이 아니니 Python FastAPI 로.
  핸디즈가 쓰는 MySQL 로. 컨테이너가 올라가면서 시드가 같이 들어가게. 논의한 구현이 들어갈 테이블 구조를 만들고 웹으로 리뷰하게."
- **AI 출력**:
  - `db/init/01_schema.sql` 18개 테이블, 세 계층(사전 10 / 원본 6 / 파생 2). `events` append-only + `tag_rules` 규칙 버전 + `taste_profiles`·`trip_keywords` 파생.
  - `db/init/02_seed.sql` 사전 계층 시드(태그 20·숙소 12·태그 73·문항 5/16·이유 15·규칙 76).
  - `api/` FastAPI: `scoring.py`(규칙 로드·가중치·재계산·점수) `main.py`(라우트 14개 + 데모 시드 + `/schema`). ORM 없음.
  - `app/index.html` 을 localStorage → API 호출로 전환(점수 계산은 서버로). `app/seed.js` 삭제. `app/schema.html` 신설(information_schema 를 세 계층으로 렌더).
  - `docker-compose.yml` 3 서비스(db healthcheck → api → web), `nginx.conf` (`/api` 프록시).
  - README 3절 전면 개정, 5절 실행 방법 갱신, 범위 판단의 "인프라 설계만" 단락 제거.
- **검토** (AI 가 실행, 사용자 미검토):
  - 1차 기동 실패: API 가 `tags` 만 보고 준비됐다고 판단해 시드 적재 중 FK 오류 → 마지막 시드 테이블(`tag_rules`·`companion_types`)을 기다리도록 수정, 데모 시드를 단계별 멱등으로 수정.
  - 2차 기동 실패: 같은 FK 오류 → DB 를 직접 조회하니 한글 시드가 전부 latin1 로 깨져 저장됨(`HEX(name)` 확인). 원인은 init SQL 에 `SET NAMES utf8mb4` 누락. 두 파일에 추가 후 볼륨 삭제·재기동으로 해결.
  - 3차: `/api/trips/{id}` 의 점수(해운대 +25·기장 +23·서면 +13 탈락)와 키워드(도심상권 −3)가 어제 JS 버전과 정확히 일치함을 확인.
  - 브라우저: 스키마 페이지 렌더 확인 → 앱에서 지민으로 기장 "넓어서" 유지 → 화면 재정렬(+32/+31) → MySQL 에서 `events` 6번째 행과 `trip_keywords` 갱신을 직접 조회해 확인. 콘솔 에러 0.
- **결과**: 채택. 사용자 리뷰 대기 — 테이블 구조는 http://localhost:8080/schema.html 에서.

### 2026-09-18 14:02 — 스키마 리뷰 반영: 18 → 11 테이블, 어휘 트리 + 해석기 프로토콜 (사용자 지적 3건)
- **요청**: (1) "테이블이 예상보다 너무 많다. 왜 이렇게 분할됐나" → AI 설명 후 (2) "고정 tags 구조는 새 취향 키워드를 소화 못 한다.
  피드백이 쌓이며 크기가 커지고 분화돼야 한다" (3) "stays 와 stay_tags 는 합쳐도 된다" (4) "tag_rules 는 피드백을 코드 규칙으로
  해석하는 건데 예외 처리 공수가 너무 크다. AI 해석을 넣되 이번엔 프로토콜만" (5) "trip_purposes 는 trips 컬럼으로 충분".
- **AI 출력**:
  - 과잉 정규화 인정: 선택지 6 테이블 + 규칙 테이블은 교과서식 반사였고 "작은 결과물"과 세 계층 설명에 방해.
  - 재설계: `tags` 를 parent/status(active·candidate·merged)/source(seed·ai·ops)/definition 을 가진 **자라는 트리**로.
    `stays.tags` JSON(다중값 인덱스), `choices` 한 테이블(kind·code·parent_code·text·tag_weights JSON), `trips.purposes` JSON.
    `tag_rules` 삭제 → `api/interpreters/` 프로토콜(`Interpreter.interpret(event, ctx) → Interpretation{tag_deltas, new_tags, rationale}`),
    결과는 새 테이블 `event_interpretations` 에 이벤트×해석기 한 행. 파생은 해석 결과의 합. `rules-v1` 구현, `ai-claude` 는
    인터페이스 + 프롬프트 초안 + NotImplemented. `docs/INTERPRETER_PROTOCOL.md` 에 입력·출력·규칙 7개·어휘 생애주기·붙이는 순서.
  - API: `/events`(원본+해석 나란히), `/tags`(트리), `/admin/reinterpret`. 프론트: 답변을 선택지 코드로, 스키마 페이지 4계층.
- **검토**: AI 가 볼륨 삭제 후 재기동 → 콘솔 에러 0 → `/api/trips/{id}` 점수(+25/+23/+13 탈락)와 키워드(도심상권 −3)가
  이전 버전과 동일 → `/api/events` 에서 각 vote 의 rules-v1 해석(태그별 evidence, "자유 문장은 AI 몫" rationale) 확인 →
  브라우저에서 멤버 "하은" 추가·취향 테스트 저장 → base 프로필 생성 확인 → 스키마 페이지 11 테이블 렌더 확인.
- **결과**: 채택 (사용자 방향 그대로). 사용자 리뷰 대기 — http://localhost:8080/schema.html , `docs/INTERPRETER_PROTOCOL.md`.

### 2026-09-18 14:18 — 스키마 도식화 + 예시 데이터 (사용자 요청)
- **요청**: "테이블 구조를 도식화해서 보고 싶다. 예시 데이터도 몇 개 들어가 있으면 좋겠다."
- **AI 출력**: (1) 리뷰용 아티팩트 페이지 — mermaid 관계도(실선 FK · 점선 데이터 흐름, 4계층 색), 테이블마다 컨테이너에서 뽑은
  실제 행 2~6개, "한 표의 여정"(이벤트 4 → 해석 → 파생) 추적. AI 해석기가 붙으면 생길 행(tags candidate, ai-claude 해석)은
  "예시"로 표시해 실제 행과 구분. (2) 같은 내용을 `docs/SCHEMA.md` 로 저장소에 (GitHub 이 mermaid 를 렌더).
- **검토**: 예시 행은 AI 가 MySQL 을 직접 조회해 옮김. 페이지 렌더는 사용자가 아티팩트에서 확인.
- **결과**: 사용자 리뷰 대기.

### 2026-09-18 14:37 — users.id · trips.id 를 정수 AUTO_INCREMENT 로 (사용자 지적)
- **요청**: "user_id 와 trip_id 가 string 인데 특별한 이유가 없다면 그냥 PK 형식으로."
- **AI 출력**: 특별한 이유 없었음(6자리 hex 는 URL 에 노출돼도 추측이 어렵다는 정도인데 인증이 없는 시연에선 무의미). 스키마의
  users·trips 와 참조 컬럼 7곳을 INT 로, trip_keywords.user_id 의 scope=trip 자리표시자를 '' → 0. API 는 lastrowid 사용,
  path/body 타입 int. 프론트는 select 값·dataset·checkbox 값을 Number 로 변환(예전 세션의 문자열 id 방어 포함).
  stays.id 는 시드 가독성 때문에 's01' 코드 유지. docs/SCHEMA.md 와 아티팩트의 예시 id 를 새 값으로 갱신.
- **검토**: AI 가 볼륨 삭제 후 재기동 → 콘솔 에러 0 → 점수·키워드 동일(+25/+23/+13) → API 로 표 추가·다음 여행 생성 →
  근거에 "오웬·이전여행, 지민·이전여행" 출처 확인. 테스트 데이터는 재기동으로 지우고 데모 시드 상태로 복구.
- **결과**: 채택.

### 2026-09-18 15:12 — 프로젝트 이름: 같이고르기 → 스테이메이트 (StayMate)
- **요청**: 이름 추천 → 5개 후보 중 사용자가 스테이메이트 선택 → "혹시 모르니 다른 서비스가 있는지 확인" → 확인 후 그대로 진행.
- **AI 출력**: 후보 5개(스테이메이트·결·모두픽·동행록·취향합)와 장단점. 동명 조사: 한국어 "스테이메이트"는 없음, 영문 StayMate 는
  호텔 투숙객 앱(staymate.com/app)·AI 예약 봇·룸메이트 앱 등 여러 건 — 같은 산업이라 실서비스명으로는 부적합하다고 보고.
  사용자가 과제용 가칭으로 확정. 8개 파일에서 이름·식별자 치환(컨테이너 staymate-*, DB staymate, 제목, localStorage 키),
  README 첫머리에 "영문 동명 서비스 있음, 실서비스명 재검토 필요" 한 줄 추가. DB 볼륨 삭제 후 재기동.
- **검토**: AI 가 재기동 → 컨테이너 3개 staymate-* 기동, DB staymate 존재, 콘솔 에러 0, 점수 동일, 페이지 title 확인.
- **결과**: 채택.

### 2026-09-18 15:22 — 취향 테스트 대리 입력 제거 (사용자 결정)
- **요청**: "테스트를 대리로 하는 건 좋지 않은 것 같다. 그 부분은 빼자."
- **AI 출력**: users.is_proxy 컬럼, API 필드, 멤버 추가 모달의 체크박스, "(대리)" 표시, 시작 안내 문구, 데모 시드의 지민 대리 표시,
  README·docs·아티팩트의 언급을 모두 제거. README "뺀 것"에 이유 추가 — 대리 입력은 결제자의 추측을 데이터로 저장하는 것이라
  "추측하지 않아도 되게 한다"는 전제와 충돌. 동행자가 없으면 테스트를 건너뛰고 3층(선별 행동)에서 시작.
- **검토**: AI 가 볼륨 삭제 후 재기동 → 에러 0, 점수 동일, users 응답에 is_proxy 없음 확인.
- **결과**: 채택. 대리 입력은 AI 가 먼저 제안했던 것(09-17 "의사결정자가 링크를 안 누르는 실패 모드 대비")이고 사용자가 뺐다.

### 2026-09-18 17:06 — 취향 테스트에 사진 카드 (사용자 요청)
- **요청**: "설문이 텍스트뿐이라 삭막하다. 사진 같은 것도 적당히 넣었으면."
- **AI 출력**: Wikimedia Commons API 로 선택지 14개에 맞는 가로 사진을 검색·다운로드(720px, 총 ~2.6MB), `app/img/CREDITS.md` 에
  원본·저작자·라이선스 표. `choices.image` 컬럼 추가(사진도 선택지 데이터), `/meta` 가 경로를 내려주고 프론트는 선택지를
  사진 카드 그리드로 렌더(선택 시 테두리·캡션 강조, 사진 없는 선택지 "상관없음·반반"은 빈 카드). 모달 하단에 출처 문구.
- **검토**: AI 가 1차 검색 결과를 contact sheet 로 확인 → 3장 부적합(불탄 숲, 주방 도구 벽걸이, 도로 표지판) → 재검색 →
  2장 또 부적합(냄비 제품컷, 뭄바이 다리) → 후보 3개씩 뽑아 보고 고름(볶음 요리 접시, 고속도로 주행). 브라우저에서
  "테스트 다시 하기" 모달을 열어 5문항 사진 카드 렌더 확인, 콘솔 에러 0. 1차 검색 코드의 mime 필터 버그(iiprop 에 mime 누락)로
  전부 걸러진 것도 AI 실수.
- **결과**: 채택. 사진 적합성은 사용자가 모달에서 한 번 봐야 함.

### 2026-09-18 17:28 — 설문 링크 공유 · 게스트 → 가입(닉네임) 흐름 (사용자 요청)
- **요청**: "멤버 추가하면 바로 설문으로 이어지는데, 설문 링크 공유 버튼을 만들고 그 주소로 처음 들어올 때 이름을 받게. 그 이름을
  나중에 가입할 때 닉네임으로 쓸 수 있게, 원하면 바꿀 수도 있게."
- **AI 출력**: users 에 nickname(NULL=게스트)·joined_at 추가. `POST /users/{id}/signup {nickname}` (최초 가입이자 닉네임 변경),
  `POST /trips/{id}/members` (초대 합류). 프론트: 헤더 "설문 링크 공유"(클립보드 복사, 보드에서 누르면 trip 파라미터),
  `?invite` 진입 시 헤더 컨트롤 숨기고 이름 입력 → 테스트 → 결과(취향 칩) + "가입하고 취향 저장하기" → 닉네임 모달(테스트 이름
  기본값) → 메인. 메인의 취향 패널에 "닉네임 바꾸기"(게스트면 "가입하고 취향 저장하기"). 표시 이름은 닉네임 우선, 게스트는 "(게스트)".
  데모 시드의 두 명은 가입 완료 상태로.
- **검토**: AI 가 API 로 게스트 생성 → 여행 합류 → 테스트 → 가입(닉네임 변경) → 보드 멤버에 새 닉네임 확인. 브라우저에서
  `?invite&trip=1` 로 진입 → "수빈" 입력 → 사진 문항 5개 → 결과 화면 → 가입 모달에 "수빈" 기본값 → "수빈이"로 변경 → 메인에서
  헤더·여행 멤버에 "수빈이" 표시 → 공유 버튼으로 링크 복사 확인. 콘솔 에러 0.
- **결과**: 채택. 원래 아이디어의 "테스트 → 결과 → 가입 유도" 가 이제 화면에 있음.

### 2026-09-18 18:02 — 설문 완료 시 바로 가입 (사용자 결정)
- **요청**: "테스트 버전이니까 설문을 완료하면 그냥 바로 가입되게."
- **AI 출력**: 초대 흐름에서 테스트 저장 직후 `signup {nickname: name}` 자동 호출. 결과 화면은 "가입됐습니다. 닉네임은 X" +
  "여행 보드로 가기/시작하기" + "닉네임 바꾸기". 별도 가입 모달 단계 제거. 스키마의 게스트 상태(nickname NULL)는 유지.
- **검토**: 브라우저 확인은 아래 항목에서 사용자 시연 시. AI 는 코드 변경만.
- **결과**: 채택.

### 2026-09-18 18:06 — 내가 멤버인 여행만 목록에 (사용자 요청, 작업 중 추가)
- **요청**: "여행에서 내가 포함되지 않은 여행은 리스트에 보이지 않도록."
- **AI 출력**: 여행 목록을 현재 사용자가 멤버인 것만 표시(프론트 필터). 빈 상태 문구 "참여 중인 여행이 없습니다. 만들거나 초대 링크로 합류".
- **검토**: AI 가 API 로 오웬만 멤버인 여행을 추가 → 브라우저에서 하은으로 보면 "10월 부산"만, 오웬으로 보면 둘 다 보임 확인.
  첫 확인 때는 브라우저가 수정 전 페이지를 캐시해 필터가 안 보였고, 새 탭 + 캐시 무력화 쿼리로 재확인. 검증 데이터는 재기동으로 삭제.
- **결과**: 채택.

### 2026-09-18 18:25 — 여행 만들기에 세부 장소·날짜 (선택) 추가 (사용자 요청)
- **요청**: "여행을 만들 때 옵셔널하게 위치를 정할 수 있게. 보통 장소 정도는 정해놓고 고르니까. 날짜도 마찬가지."
- **AI 출력**: trips 에 location(자유 입력)·start_date·end_date 추가(전부 NULL 허용). 폼에 "세부 장소" 텍스트와 체크인/체크아웃
  date 입력(체크인 고르면 체크아웃 최소값). 카드·보드 머리에 "강원 속초 해변 · 11/07–11/09 (2박)" 식으로 표시.
  trip_created 이벤트 payload 에도 포함. 지역 필터는 그대로이고 장소·날짜는 후보 필터에 쓰지 않음(좌표·재고·요금 데이터 없음) —
  README "뺀 것"에 이유와 함께 명시.
- **검토**: AI 가 재기동 후 API 로 장소·날짜 있는 여행과 없는 여행을 각각 생성해 저장·조회 확인. 화면은 아래 브라우저 확인.
- **결과**: 채택.

### 2026-09-18 18:52 — 숙소 실데이터 스크래핑 + 리뷰 키워드 (사용자 요청)
- **요청**: "숙소 정보를 최대한 많이 스크래핑해서 넣어볼 수 있나. 리뷰를 분석해 5개 정도 키워드를 뽑아 저장했으면." → 출처 검토 후
  사용자가 hotels.com·야놀자를 물었고, AI 가 확인해 야놀자로 결정.
- **AI 출력**:
  - 출처 검토: urbanstay.co.kr(리뷰 없음) · Trip.com(약관) · Agoda/Tripadvisor/여기어때(403) · hotels.com(429, 리뷰 API robots 금지) ·
    야놀자(상세 페이지 서버 렌더링, robots 는 /search/·/reviews/ 만 금지). 야놀자 상세 페이지만, 검색은 외부 웹 검색으로.
  - `scripts/scrape_yanolja.py`: 웹 검색으로 모은 ID 20개를 지점당 1회(2초 간격) 요청. 이름·브랜드·지역·주소·최대 인원·평점·리뷰 수·
    편의시설·객실 타입·원본 URL 파싱. 리뷰 문장 30~60건을 키워드 사전(25개 패턴, 극성 포함)으로 훑어 상위 5개+빈도만 저장.
    **리뷰 원문·HTML 미저장.** `stays.tags` 는 이름·브랜드·편의시설·객실·리뷰 키워드(3건 이상)에서 규칙 도출.
    결과 → `data/stays_scraped.json` + `db/init/03_stays.sql`(기동 시 네트워크 불필요). 가상 숙소 12개 제거.
  - 스키마: stays 에 brand·address·min_price·rating·review_count·amenities·room_types·review_keywords·description·source_url.
    지역 선택지를 실제 분포로(서울·경기·인천·부산·강원·충남·제주). 리뷰에서 나온 어휘 5개(청결·조용함·편의점·친절·비대면체크인)를 tags 에 추가.
  - 프론트: 숙소 카드에 평점·리뷰 수·리뷰 키워드 칩·야놀자 링크. README 에 "숙소 데이터" 절(출처·저장 범위·한계).
- **검토** (AI 실행):
  - 1차: 20개 중 2개(레지던스 동탄·부티크 익선)가 상세가 아닌 페이지 → 재시도 후 제외. "전망" 키워드가 전 지점 상위 → 정규식 `뷰`가
    "리뷰"에 매칭되던 버그 → `(?<!리)뷰` 로 수정. 가격이 위약금·주차비 금액을 잡음 → 정책 줄 제외했더니 1박가는 HTML 에 없음(클라이언트
    렌더링) → price_tier 를 브랜드·객실로 추정하고 README 에 한계로 명시.
  - 2차: 객실 안내 문구("전망(뷰) 및 소품은 사진과 상이할 수 있습니다")가 리뷰로 잡힘 → 제외 패턴 추가. 소개 문구 추출이 페이지 제목·
    면책 문구·리뷰를 섞어 잡음 → 저장하지 않기로(컬럼만 남김).
  - 3차: 지역 선택지 교체가 안 먹어 '경기·인천' 지점이 필터에 안 잡힘 → 시드 재수정. 재기동 후 18개 적재, 데모 여행(부산) 후보 5개
    자동 추천, 키워드 패널·정렬 동작 확인.
- **결과**: 채택. 사용자 리뷰 대기 — 키워드 품질(전망·침구가 넓게 잡힘)과 tier 추정은 사용자가 봐야 함.

### 2026-09-18 18:57 — DB 초기화 뒤 사라진 여행을 기억하는 브라우저 상태 방어
- **요청**: (사용자 요청 없음. AI 가 검증 중 발견)
- **AI 출력**: 브라우저 localStorage 가 이전 여행 id 를 기억한 채 DB 를 초기화하면 보드가 404 를 띄우고 빠져나갈 길이 없었음.
  404 면 여행 목록으로 자동 복귀, 그 외 오류엔 "여행 목록으로" 버튼.
- **검토**: 자동화 브라우저의 클릭이 "보드 열기"에 전달되지 않아 한동안 회귀로 오인. 페이지 스크립트로 직접 클릭해 핸들러가
  정상 동작하고 실데이터 보드(평점·리뷰 키워드·야놀자 링크)가 렌더되는 것을 확인. 콘솔 에러 0.
- **결과**: 채택.

### 2026-09-18 19:59 — 추천 카드를 "이 숙소가 왜 이번 여행에 맞는가" 중심으로 (사용자 지적)
- **요청**: "지금 추천은 '이 숙소의 키워드는 이렇습니다'에 가깝다. 힘을 받으려면 '왜 이번 여행에 적절한지'를 보여줘야 한다."
- **AI 출력**: 점수 근거(parts)를 사람이 읽는 문장으로 재구성. 순서: 멤버별 "○○이 중요하게 보는 A·B가 있어요"(취향 테스트) →
  "커플·휴양 여행에 필요한 C"(컨텍스트) → "이번 선별에서 ○○이 남긴 숙소와 같은 D" / "지난 커플 여행에서 좋아한 E"(학습) →
  "리뷰에서도 청결 8건·해변 7건 언급"(맞는 태그와 겹치는 리뷰 키워드만) → ✕ "○○이 이번에 피한 F 특징" / "○○이 원하는 G는 없어요".
  머리말은 ✓ 개수로 "잘 맞아요 / 맞는 점이 있어요 / 잘 안 맞아요". 태그 칩과 계산식은 접힘(details)으로 강등.
  보드 API 에 멤버 base 프로필 추가(원하는데 없는 것 계산). 받침 기반 조사 처리(이/가·은/는). ±1 약신호는 문장에서 제외.
- **검토**: AI 가 페이지 스크립트로 보드를 열어 카드 문장 확인. 1차: 조사 오류("주차이 있어요") → 받침 판별 추가. 2차: 마지막 태그
  중복("실속실속이") → 조사만 붙이도록 수정. 약신호 제외 전에는 "지민이 피한 가성비·실속"처럼 탈락 숙소 특징 −1 이 문장에 새어 나옴.
- **결과**: 채택. 문장 톤은 사용자 확인 필요.

### 2026-09-18 20:09 — 한 줄(note) 저장·해석: notes-v1 해석기 (사용자 지적)
- **요청**: "한 줄 남기기도 저장하고 분석해야 합니다."
- **AI 출력**: 저장은 이미 events.payload.note 에 되고 있었고 해석이 없었음. 자유 문장을 읽는 두 번째 규칙 해석기 `notes-v1`
  (`api/interpreters/notes_v1.py`): 키워드 사전 22패턴 × (유지 표 가중치, 탈락 표 가중치), "없어서·아쉬움" 표현이면 원하는 것으로
  + 방향, confidence 0.7. `ACTIVE_INTERPRETERS = [rules-v1, notes-v1]`. 같은 이벤트에 두 해석 행. 보드의 표 표시에
  "→ 한 줄 해석: 도심상권 −2, 조용함 +1" 을 붙임. 프로토콜 문서·README 갱신(LLM 해석기는 여전히 프로토콜만).
- **검토**: AI 가 재기동 후 /api/events 로 데모 표(지민 "서면은 너무 복잡해")에 notes-v1 행이 생기고 trip_keywords 의
  도심상권이 −3 → −5 로 바뀐 것 확인. 보드 응답의 note_tags 확인.
- **결과**: 채택.

### 2026-09-18 20:09 — 지도에서 고르기 (사용자 요청, 작업 중 추가)
- **요청**: "카탈로그에서 더 담기도 추천 리스트인데 지도에서 고르기가 있었으면. 누르면 지정한 여행지 중심으로 바로 나왔으면."
- **AI 출력**: 야놀자 페이지의 위도·경도를 스크래퍼에서 추출(18/18), `stays.lat/lng` 추가. Leaflet(cdnjs) + OpenStreetMap 타일로
  모달 지도. 중심 = 여행의 세부 장소가 숙소 이름·주소에 포함되면 그 숙소(팝업 자동 열림) → 아니면 지역 중심 좌표 → 아니면 전체.
  핀 = 후보(초록)·담을 수 있음(회색, 점수 클수록 큼). 팝업에 이름·평점·"이번 여행에 맞는 이유" 상위 3줄·담기 버튼.
  보드 API 의 more 를 전체로 바꾸고 목록은 6개만, 나머지는 "지도에서".
- **검토**: AI 가 브라우저 스크립트로 모달을 열어 Leaflet 로드·핀 7개·타일 20장·"부산 중심" 제목 확인. 팝업·담기·장소 중심은 아래 확인.
  인터넷 없는 시연이면 타일·라이브러리가 안 뜬다 — README 실행 절에 명시. 이어서 회색 핀 클릭 → 팝업(이유 3줄 + 담기 버튼) 확인,
  세부 장소 "해운대" 여행에서 지도가 "부산 해운대 중심" 으로 열리고 해운대역 팝업이 자동으로 열리는 것 확인.
- **결과**: 채택.

### 2026-09-18 20:34 — 보드를 지도 중심 3단(목록·지도·상세)으로 (사용자 요청)
- **요청**: "리스트 중심인데 지도 중심으로. 좌측 리스트, 가운데 지도에 숙소 위치, 클릭하면 오른쪽에 정보."
- **AI 출력**: renderBoard 전면 교체. 왼쪽 목록(후보/탈락/더 담을 수 있는 숙소, 항목마다 점수·적합도 한 줄) · 가운데 Leaflet 지도
  (후보 초록·담을 수 있음 회색·탈락 주황, 핀 크기 = 적합도, 툴팁) · 오른쪽 상세(이유 문장·리뷰 키워드·주소·표와 한 줄 해석·
  유지/탈락/취소 또는 담기). 미선택 시 상세에 여행 컨텍스트·학습 키워드. 선택 숙소와 지도 시점은 다시 그려도 유지. 지도 모달 삭제.
  main 폭 1080 → 1400. 좁은 화면은 세로 쌓임.
- **검토**: AI 가 브라우저 스크립트로 목록 클릭 → 상세 전환, 회색 핀 클릭 → "후보에 담기" 버튼 있는 상세, 선택 강조 1개 확인.
  첫 렌더에서 3단이 1080px 에 눌려 지도가 좁게 나옴 → main 폭 규칙 위치 문제(먼저 선언된 규칙에 밀림) 수정. 상세에서 유지 표 →
  모달 → 저장 후 같은 숙소가 선택된 채 상세에 "오웬: 유지 · 넓어서" 가 붙고 목록 강조가 유지되는 것 확인. 검증 데이터는 재기동으로 삭제.
- **결과**: 채택.

### 2026-09-18 20:38 — 지도 핀 5단계 추천 등급 색 (사용자 요청)
- **요청**: "지도에서 5개 정도의 색으로 강력추천·추천·중간·비추천·매우비추천 구분. 강력추천 쪽이 눈에 잘 띄게."
- **AI 출력**: 등급 = 그 여행 최고점 대비 비율(≥80% 강력추천, ≥55% 추천, ≥30% 중간, ≥0 비추천, 음수 매우비추천).
  색·크기: 강력추천 진한 빨강 r15 → 추천 주황 r12 → 중간 노랑 r10 → 비추천 회청 r8 → 매우비추천 옅은 회색 r6. 낮은 등급부터 그려
  강력추천이 위에. 후보 여부는 테두리(진한 검정 vs 흰색), 탈락은 반투명. 목록·상세에 같은 등급 배지, 범례 갱신.
- **검토**: AI 가 브라우저 스크립트로 등급 배지(강력추천 2·추천 1·중간 4)와 핀 fill 색이 등급과 일치함을 확인, 스크린샷으로 빨강 핀이
  가장 크게 보이는 것 확인. 데모 데이터엔 비추천·매우비추천이 없어 그 두 색은 범례로만 확인.
- **결과**: 채택. 등급 경계(80/55/30%)는 손으로 정한 값 — 사용자 판단 필요.

### 2026-09-18 20:44 — 간격 정리 + 조건 밖 숙소도 지도에 (사용자 지적 2건)
- **요청**: "row 간격이 다닥다닥 붙고 전반적으로 안 예쁘다." / "서울 쪽은 숙소가 아예 없는 것처럼 나온다. 이유를 알려주고, 지도를 축소하면
  다른 지역 숙소가 나오게."
- **AI 출력**: (1) 원인: 보드가 여행 조건(지역·인원)에 맞는 숙소만 올렸음. 사용자가 만든 "11월 서울 · 친구 5명" 여행은 서울 유일 지점
  명동(최대 2인)도 인원 초과라 후보 0. 카탈로그의 서울 지점이 1개뿐인 것(익선 페이지 실패)도 원인. (2) 보드 API 가 조건 밖 숙소를
  `others`(사유: 지역/인원)로 같이 주고, 지도에 점선 테두리·옅은 색 핀으로 표시, 목록에 접힌 "이번 여행 조건 밖 N", 상세에
  "조건 밖 — 사유. 그래도 담을 수 있어요". 등급 기준(최고점)은 조건 안 숙소끼리. (3) CSS: 본문 14.5px·행간 1.6, 패널 패딩 22/24,
  폼 행 간격 14px, 칩·버튼·카드·목록 항목 패딩 확대, 라운드 정리.
- **검토**: AI 가 사용자가 만든 서울 여행에서 핀 18개(전부 점선), 조건 밖 섹션 18 표시 확인. 축소 후 렌더는 아래에서 재확인.
- **결과**: 채택.

### 2026-09-18 20:51 — 여행 삭제 (사용자 요청)
- **요청**: "여행 삭제도 추가해주세요."
- **AI 출력**: 소프트 삭제 — `trips.deleted_at` 추가, `DELETE /api/trips/{id}?user_id=`(멤버만, 403), `trip_deleted` 이벤트 기록.
  목록·보드 조회에서 삭제된 여행 제외. 이벤트·해석·파생은 남겨 취향 학습은 유지. 프론트: 목록 카드와 보드 머리에 "삭제" 버튼,
  브라우저 확인창 대신 첫 클릭에 "정말 삭제할까요? 다시 누르면 삭제"(3초 후 원복), 두 번째 클릭에 삭제.
- **검토**: AI 가 API 로 여행 생성 → 비멤버 삭제 403 → 멤버 삭제 200 → 목록에서 사라지고 마지막 이벤트가 trip_deleted 임을 확인.
  브라우저에서 두 번 클릭 흐름 확인(아래). **스키마 변경(ENUM·컬럼)으로 DB 볼륨을 다시 만들면서 사용자가 만든 "11월 서울" 여행이
  지워짐** — 사용자에게 알림. 이후 스키마 변경 없이는 `down -v` 를 하지 않기로.
- **결과**: 채택.

### 2026-09-18 21:27 — 카탈로그 96개로 확장 + 제목에서 지역 자동 세팅 (사용자 요청)
- **요청**: "총합이 100개 정도는 돼야 시연에 불편이 없다." / "여행 제목에서 지역명이 나오면 자동으로 지역이 세팅되게."
- **AI 출력**: (1) 핸디즈 지점은 30개뿐이라 일반 숙소를 더함. 야놀자의 공개 목록 페이지("○○ 근처 호텔·리조트 추천 베스트 10",
  robots 허용 경로)에서 ID 를 모으는 `collect_extra_ids`(도시·구 24곳, 도시당 2~8개 상한) → 같은 파서로 긁음. `is_handys` 컬럼으로
  구분, 브랜드 기반 태그는 핸디즈에만, 일반 숙소는 이름·편의시설·객실·리뷰 키워드로. 지역 선택지에 대구·대전·광주·전남·경북 추가.
  결과 96개(핸디즈 18 + 일반 78): 서울 33 · 부산 24 · 강원 15 · 경기·인천 12 · 대구 4 · 대전 3 · 광주 3 · 제주 1 · 충남 1.
  (2) 제목 입력 시 키워드 표(강남·홍대·해운대·속초·애월 … 40개)로 지역과 세부 장소를 자동 세팅. 직접 바꾼 뒤엔 덮어쓰지 않음.
  목록·상세에 "핸디즈" 배지.
- **검토**: 목록 페이지 요청이 12번째쯤부터 403(속도 제한) → 제주·수원·여수·경주·천안은 못 모음. 8초 간격으로 3회 재시도해도
  403 이라 포기(제주는 핸디즈 제주공항 1개뿐). 재기동 후 DB 96개, 데모 부산 여행에서 후보 5·더 담기 19·조건 밖 72 확인.
  브라우저 스크립트로 제목 "11월 속초 친구여행" → 강원/속초, "제주 애월 가족" → 제주/애월, 지역을 손으로 대구로 바꾼 뒤
  "강남 출장" → 지역 유지·장소만 강남 자동 세팅 확인. README 의 "가상 숙소 12개" 문장이 남아 있던 것 발견해 수정.
- **결과**: 채택. 100개에 4개 모자람. 제주가 1개인 것은 시연 시 알아둘 것.

### 2026-09-18 21:34 — 인원 미달 시 초대 배너 (사용자 요청)
- **요청**: "인원이 n명인데 멤버가 그보다 모자랄 수 있다. 그럴 땐 '공유해서 친구를 초대해보세요!' 메시지와 링크가 뜨고, 친구가
  퀴즈→가입을 마치면 자연스럽게 해당 여행에 들어가게."
- **AI 출력**: 초대 흐름(`?invite&trip=ID` → 이름 → 테스트 → 자동 가입 → 합류 → 보드)은 이미 있어서, 보드 머리에 멤버 수 < 인원일 때
  배너("N명 중 M명만 참여 중이에요. 링크를 공유해서…") + 초대 링크 + 복사 버튼을 추가. 목록 카드에도 "(M/N명) · 친구를 초대해보세요".
  헤더 공유 버튼과 링크 생성 함수 공용화.
- **검토**: AI 가 4명 여행을 1명으로 만들고 보드에서 배너·링크·복사 버튼 확인 → 초대 링크로 "민준" 진입 → 테스트 → 자동 가입 →
  "여행 보드로 가기" → 그 여행 보드가 민준 계정으로 열리고 멤버 2/4, 배너 유지 확인. 콘솔 에러 0.
- **결과**: 채택. 검증에 쓴 "12월 강릉 친구"(2/4명)와 멤버 민준은 시연에 쓸 만해 DB 에 남겨 둠.

### 2026-09-18 21:40 — 댓글 + 링크 미리보기, 좋아요, 모달 z-index (사용자 요청 3건)
- **요청**: "상세에 자유 댓글. 식당 네이버 지도 링크를 남기면 그 가게 정보를 보여줬으면." / "좋아요도. 누가 누르면 왼쪽 후보에
  '○○이 좋아하는 숙소예요!'" / "멤버 추가를 누르면 맵이 하이라이트되는 느낌인데 잘못 동작하는 것 아닌가."
- **AI 출력**: (1) `stay_comments` + `events.comment`, `POST/GET /trips/{id}/comments`. 본문의 URL 을 서버가 열어 OpenGraph
  제목·설명·이미지·사이트를 읽어 저장(최대 3개). 비ASCII URL 퍼센트 인코딩. 네이버 지도(map.naver.com·m.place.naver.com)는
  클라이언트 렌더링이라 OG 에 장소 정보가 없음 → "네이버 지도 장소 #ID" 카드로 대체하고 README 에 실서비스 대안(지역검색·카카오 로컬 API) 명시.
  (2) `stay_likes` + `events.like/unlike`, rules-v1 이 like 를 숙소 특징 +1 로 해석, 파생 재계산에 포함. 상세에 ♥ 토글, 목록·상세에
  "♥ ○○이 좋아하는 숙소예요!". (3) 원인: 모달 z-index 10 < Leaflet 컨트롤 1000 → 모달이 지도 뒤에 깔려 "하이라이트"처럼 보임.
  모달 5000, 헤더 1100 으로.
- **검토**: API 로 좋아요 → 보드 응답 likes 와 멤버 키워드(역세권 +1 등) 반영 확인. 위키백과 링크 미리보기 제목·이미지 확인, 네이버 지도
  링크는 대체 카드 확인. 브라우저: 목록 "♥ 지민이 좋아하는 숙소예요!", 멤버 추가 모달이 지도 위에 뜨는지(elementFromPoint), 상세의
  ♥ 버튼·댓글 목록·미리보기 카드 확인(아래).
- **결과**: 채택. **스키마 변경 3번(삭제·댓글·좋아요)마다 DB 를 다시 만들어 사용자가 만든 "11월 서울" 여행이 반복해서 지워짐** —
  사용자 질문에 원인으로 설명. 이후 스키마 변경은 ALTER 로 하고 볼륨은 지우지 않기로.

### 2026-09-18 21:43 — 계산 근거의 특징 태그에 겹침 색 (사용자 요청)
- **요청**: "특징 태그·계산 근거에서 겹치는 키워드는 색깔을 눈에 잘 띄게."
- **AI 출력**: 상세의 접힌 "특징 태그·계산 근거"에서 숙소 태그마다 이번 여행 점수 기여(parts 순합)를 계산해 양수는 진한 초록(+n),
  음수는 빨강(−n), 0 은 회색으로. 한 줄 범례 추가.
- **검토**: 브라우저에서 해운대역 상세를 열어 칩 클래스(hit/miss)와 값 표시 확인(아래).
- **결과**: 채택.

### 2026-09-18 21:46 — 좋아요 +5 (사용자 요청)
- **요청**: "여행 멤버 중 좋아요를 누른 숙소라면 점수에 +5."
- **AI 출력**: 보드 점수에 멤버 좋아요 한 명당 +5 (`LIKE_BONUS`). 근거 목록에 kind=like 로 들어가고, "이번 여행에 맞는 이유"에
  "♥ ○○이 좋아요를 눌렀어요 +5" 줄. 취향 학습(+1)과는 별개. "누른 숙소라면 +5"를 사람 수만큼으로 해석 — 한 번만 +5 로 바꾸기 쉬움.
- **검토**: API 로 지민이 좋아요 누른 해운대역 total 34 → 39, parts 에 지민·좋아요 +5 확인. 브라우저에서 순위·근거 줄 확인(아래).
- **결과**: 채택.

### 2026-09-18 21:48 — 묵었던 숙소 표시 + 결정 숙소 + 시연용 이력·멤버 10명 (사용자 요청)
- **요청**: "멤버가 예전에 갔던 숙소가 후보에 뜨면 표시. 여행 데이터가 없으니 가짜로 만들어 가능하게. 유저도 열 명 정도로."
- **AI 출력**: "갔던"을 알려면 결정이 필요해 `trips.chosen_stay_id` + `POST /trips/{id}/decide` + `trip_decided` 이벤트, 상세에
  "이 숙소로 결정" 토글, 머리·목록에 결정 표시. 보드 응답에 후보별 `visited`(멤버가 결정했던 과거 여행). 목록·상세에
  "🏠 오웬·지민이 묵은 곳 · 작년 10월 부산 커플 (2025년 10월)". **DB 는 지우지 않고 ALTER TABLE** 로 컬럼·ENUM 추가.
  `seed_history`: 멤버 8명 추가(총 10, 각자 취향 테스트), 과거 여행 4개(부산 커플·속초 친구·제주 커플·강릉 가족, 결정 숙소·표·좋아요 포함),
  진행 중 여행 1개(12월 강릉 친구 5명). 신규 기동 시 자동, 기존 DB 는 `POST /admin/seed-history`.
- **검토**: 엔드포인트로 이력 시드 → 사용자 10명, 과거 여행 4개에 결정 숙소 확인, 데모 "10월 부산"에서 부산송도해변에
  visited(오웬·지민, 작년 10월 부산 커플) 붙는 것 확인. 사용자가 다시 만든 "11월 서울"은 유지됨. 브라우저에서 목록·상세의 🏠 줄,
  "이 숙소로 결정" → 머리에 "✔ 부산송도해변 로 결정"·목록 "✔ 이 여행의 결정 숙소" → 취소까지 확인. 멤버 셀렉트 10명. 이력이 생기자
  근거에 "지민이 지난 커플 여행에서 좋아한 바다뷰·가성비" 줄이 실제로 등장.
- **결과**: 채택.

### 2026-09-18 21:51 — 좌상단 로고 클릭 → 여행 목록 (사용자 요청)
- **요청**: "좌상단의 스테이메이트를 누르면 목록 화면으로."
- **AI 출력**: 헤더 제목에 클릭 핸들러(목록 뷰로 전환, 맨 위로 스크롤). 초대 화면에서는 index.html 로 이동.
- **검토**: 정적 파일 변경만. 사용자가 새로고침 후 확인.
- **결과**: 채택.

### 2026-09-18 21:52 — 목록 화면 순서: 취향 → 새 여행 만들기 → 여행(최신순) (사용자 요청)
- **요청**: "오웬의 취향 / 새 여행 만들기 / 여행 순으로 메뉴 구성을 다시. 리스트는 최신순."
- **AI 출력**: renderTrips 의 섹션을 취향 → 새 여행 만들기(독립 패널) → 여행 목록 순으로 재배치, 목록은 created_at 내림차순 정렬 보장.
- **검토**: node 로 스크립트 문법 검사, 브라우저에서 섹션 순서와 목록 순서 확인(아래).
- **결과**: 채택.

### 2026-09-18 21:58 — 태그 "편의점" → "편의점 인접", ±1 약신호 숨김 (사용자 질문)
- **요청**: "편의점 키워드가 뭔가" → "편의점 인접으로 하자. 그런데 편의점 −1 이면 편의점을 안 좋아한다는 건가? 세탁기 −1 도 무슨 뜻인지 모르겠다."
- **AI 출력**: 편의점 = 리뷰에서 "건물에 편의점" 언급을 세어 만든 태그. −1 은 싫어함이 아니라 탈락시킨 숙소가 가진 특징 전부에 붙는 −1
  (초기 설계 "탈락 숙소의 특징 데이터"의 가장 단순한 형태). 그대로 노출하니 오해됨. 조치: 태그 이름을 "편의점 인접"으로(실행 중 DB 는
  UPDATE 로, 시드·스크래퍼·JSON 도), 취향 패널·여행 키워드 패널·멤버별 줄에서 |가중치| 2 이상만 표시(근거 문장은 이미 그랬음).
  README 에 뜻과 표시 규칙 명시.
- **검토**: DB 에서 태그 rename 후 stays 2곳·프로필·키워드·해석 JSON 치환 확인, 오웬 프로필에서 2 이상만 남는 것 확인.
- **결과**: 채택. 근본 개선(탈락 특징 −1 을 "유지 숙소엔 없는 특징"으로 한정)은 다음 단계로 README 에 둘 만함 — 시간상 미구현.

### 2026-09-18 22:02 — 탈락은 이유 키워드만 − (사용자 결정, 학습 규칙 변경)
- **요청**: "탈락에는 이유가 있으니 그 숙소의 모든 특징을 negative 로 잡으면 안 된다. 사용자가 이유를 명시할 때만 그 이유의 키워드를 빼서 −."
- **AI 출력**: rules-v1 의 vote(drop) 에서 숙소 특징 전체 −1 제거. 탈락은 이유 선택지 가중치(그리고 notes-v1 의 한 줄)만. 유지는 특징 +1 유지.
  `POST /admin/reinterpret` 로 기존 이벤트 전부 재해석·파생 재계산(원본 불변 원칙이 실제로 쓰인 사례). README·프로토콜 문서 갱신.
- **검토**: 재해석 후 탈락 이벤트의 해석이 "교외조용 +1, 도심상권 −2"(이유만)로 바뀐 것, 지민 커플 프로필에서 세탁기·편의점 등
  −1 이 사라진 것 확인.
- **결과**: 채택. 이전 설계("탈락 숙소의 특징 데이터")를 사용자가 다듬은 것.

### 2026-09-18 22:24 — choices.tag_weights 를 tags.id 참조 배열로 (사용자 지적)
- **요청**: "{\"도시야경\":3} 처럼 태그 이름을 키로 쓰는 건 정규화가 안 좋다. tags 테이블 id 와 매칭한 {tag_id, score} 형식으로."
- **AI 출력**: tags 에 숫자 `id`(AUTO_INCREMENT, UNIQUE) 추가. choices.tag_weights 를 `[{"tag_id":n,"score":w}]`, 범주 규칙은
  `[{"parent_id":n,"score":w}]` 로. 실행 중 DB 는 ALTER + 42행 UPDATE(볼륨 미삭제), 시드 SQL 은 tags 에 id 를 명시하고 JSON 변환.
  API load_choices 가 id→이름으로 풀어 해석기 코드는 그대로. 문서·도식 갱신. `name` 이 다른 테이블 FK 의 키인 것은 유지
  (전면 id 전환은 남은 시간에 위험하다고 판단, README/다음 단계).
- **검토**: 변환 전후 지민 프로필을 reinterpret 로 비교 → 동일. meta 응답 정상.
- **결과**: 채택.

### 2026-09-18 22:27 — event_interpretations.tag_deltas 도 tags.id 참조로 (사용자 지적)
- **요청**: "tag_deltas 도 같은 이슈가 있는 것 같다. 확인해서 수정."
- **AI 출력**: 맞음 — 태그 이름을 그대로 저장하고 있었음. 저장 시 이름→`tags.id` 로 바꿔 `[{tag_id, weight, confidence, evidence}]` 로 넣고,
  읽는 쪽(파생 합산 `deltas_of`, 보드의 한 줄 해석, `/api/events`)에서 이름을 결합. 해석기 인터페이스는 이름 기반 그대로(해석기가 DB id 를
  몰라도 되게) — 프로토콜 문서에 규칙 8로 명시. 구형식 행도 읽을 수 있게 호환. 기존 행은 `reinterpret` 로 전부 새 형식으로 재작성.
- **검토**: 변환 전후 지민 프로필 동일. DB 의 이벤트 4 해석 행이 tag_id 형식, `/api/events` 에 tag_id 와 이름이 함께 나옴.
  사용자 요청("바뀐 데이터에 맞게 코드 레벨 점검")으로 태그 이름/ID 를 다루는 코드를 전수 grep 하고, 임시 유저로 취향 테스트 →
  여행 → 탈락(이유 d_price + 한 줄 "주방이 없어서 아쉬움") → 좋아요를 API 로 돌려 base 프로필·여행 키워드·커플 학습·해석 행이
  기대값(탈락: 가성비 +2·업스케일 −1 만, 한 줄: 주방 +2, 좋아요: 특징 +1)과 일치함을 확인. 구형식 행 0. 임시 데이터 삭제.
- **결과**: 채택.

### 2026-09-18 22:40 — README §2 "일부러 뺀 것과 이유" → "발전 방향"
- **요청**: 사용자가 절 제목을 "발전 방향"으로 바꾸고, 인스타그램 항목을 "소셜미디어 플랫폼과 연동, 숙소/여행 사진 업로드를 유도하여 앱 홍보, 바이럴 유도" 로 교체하라고 지시.
- **AI 출력**: 지시대로 제목·인스타그램 항목 교체. 같은 절의 "초대 링크·실시간 동기화·인증" 항목은 초대 링크가 이미 구현돼 있어 사실과 어긋나므로
  "실시간 동기화·인증"(초대 링크는 있고 로그인은 없음)으로 고치고, "더 하면 할 것" 2번도 "인증과 실시간 동기화"로 맞춤. 이 두 수정은 AI 판단.
- **검토**: 사용자 확인 대기. AI 는 README 에서 "일부러 뺀" 잔여 표현이 없는지 grep.
- **결과**: 채택 (사용자 지시분). 초대 링크 문구 수정은 사용자 확인 필요.
  이어서 사용자가 "발전 방향"과 "더 하면 할 것"을 직접 합치고(뺀 것의 이유 항목들은 삭제, 번호 목록만 남김) 포맷 통일만 요청 →
  AI 가 번호 목록 하나(굵은 제목 — 설명)로 정리, 소셜미디어 항목을 1번으로. 항목 순서·내용은 사용자가 정한 대로.

### 2026-09-18 22:48 — README 문체를 명사형 종결로 통일
- **요청**: "전반적인 말투가 불편. 저장한다 → 저장 식으로 일관되게".
- **AI 출력**: README 전문(1~5절)을 "~한다/~이다" 서술형에서 명사형·"~음/~함" 종결로 재작성. 내용·순서·표는 유지. 인용문("어휘가 자란다" 등)과 코드 블록은 그대로.
  같이 고친 사실 오류: 테이블 수 11 → 13(좋아요·댓글 테이블 추가분 반영), events 타입 목록에 like/unlike/comment/trip_decided/trip_deleted 추가,
  선택지 62 → 60, 숙소 "약 100" → 96, 원본 계층에 stay_likes·stay_comments 추가. §4 인스타그램 문장은 "이번 범위에서 제외, 발전 방향으로 이동"으로 맞춤.
- **검토**: AI 가 grep 으로 "~다." 종결 잔여 0건 확인(인용문 제외). 수치는 DB 에서 SELECT COUNT 로 대조. 사용자 확인 대기.
- **결과**: 채택 대기. 이전 판은 scratchpad 에 백업.

### 2026-09-18 22:53 — README 작업자 명시, 1인칭 → Owen
- **요청**: 맨 위에 작업자 Owen 명시, "내가/나의 판단" 같은 1인칭 표현을 Owen 으로 치환.
- **AI 출력**: 머리말에 "**작업자: Owen.**" 추가. §4 의 "내가" 7곳과 소제목 "사람이" 2곳을 "Owen 이" 로 치환. §2 표의 "본인이 직접 응답"은 사용자(앱 이용자)를 가리키므로 유지.
- **검토**: grep 으로 "내가/나의/나는" 잔여 0건 확인. 사용자 확인 대기.
- **결과**: 채택 대기.

### 2026-09-18 22:58 — 컨텍스트별 8개 커밋
- **요청**: 필요한 것만 컨텍스트별로 나눠 커밋. 문서 커밋은 README·CLAUDE.md·AI_LOG 만, 훅 설정과 AI_PROMPTS 는 별도 커밋.
- **AI 출력**: 인프라 / DB / 백엔드 / 프론트 / 숙소 데이터 / 문서 / 훅 / 프롬프트 기록 순으로 8개 커밋. `.gitignore` 에 `__pycache__/` 추가해 pyc 제외.
  AI_LOG 의 `_notes.local/BRIEF.md` 경로 언급은 "로컬 브리프 메모" 로 치환(제출물 독자가 열 수 없는 경로).
- **검토**: 사용자가 묶음 구성을 두 번 검토해 문서 커밋 범위를 좁힘. 커밋 후 `git status` 잔여 0, `_notes.local/` 미포함 확인.
- **결과**: 채택. 이 항목 자체와 이후 프롬프트는 마지막에 한 번 더 커밋 필요.

### 2026-09-18 23:04 — AI_PROMPTS.md 커밋 취소 (AI 오독)
- **요청**: "6번에서는 README·CLAUDE·AI_LOG 만 커밋" 지시를 AI 가 "훅·AI_PROMPTS 는 따로 커밋" 으로 오독해 AI_PROMPTS.md 를 공개 저장소에 푸시. 사용자가 지적.
- **AI 출력**: 해당 커밋을 히스토리에서 제거(reset 후 force push). `.gitignore` 에 `AI_PROMPTS.md` 추가. README §4 와 CLAUDE.md 의 언급을 "로컬 기록, 제출물 미포함" 으로 수정.
- **검토**: 사용자 지적으로 발견. AI 는 force push 후 `git ls-remote` 와 원격 파일 목록으로 AI_PROMPTS.md 부재 확인.
- **결과**: 수정 후 채택. 훅 설정 커밋(.claude/)은 남겨 둠 — 유지 여부는 사용자 판단.

### 2026-09-18 23:05 — 훅 설정 커밋 제거
- **요청**: 훅 설정(.claude/) 커밋도 제출물에서 제외.
- **AI 출력**: rebase --onto 로 해당 커밋을 히스토리에서 제거, `.gitignore` 에 `.claude/` 추가, force push. 훅은 로컬에서 계속 동작.
- **검토**: AI 가 push 후 원격 파일 목록에서 .claude/·AI_PROMPTS.md 부재 확인.
- **결과**: 채택.
